import { Press_Start_2P } from "next/font/google";

/** Fonte pixel só para a camada escritório RPG — não afeta o resto do app. */
export const rpgPixelFont = Press_Start_2P({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});
